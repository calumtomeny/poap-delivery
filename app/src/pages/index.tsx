import React from 'react';
import { Stack } from '@chakra-ui/core';

// ui components
import Header from 'ui/components/Header';

const Home = () => {
  return (
    <Stack spacing={24}>
      <Header />
    </Stack>
  );
};

export default Home;
